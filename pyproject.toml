[project]
name = "week09"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.128.2",
    "uvicorn>=0.40.0",
]

[tool.uv.workspace]
members = [
    "fastapi-lab",
    "wk11/pygameapp",
]
